{
  "name": "shopify-app-next",
  "version": "2.0.1",
  "main": "web/dist/index.js",
  "license": "MIT",
  "scripts": {
    "dev": "shopify app dev",
    "info": "shopify app info",
    "build": "shopify app build",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy",
    "tunnel": "cloudflared tunnel run shopify-dev",
    "sync:shopify-app-toml": "node scripts/generate-shopify-app-toml.cjs",
    "lint": "pnpm -C web lint",
    "lint:fix": "pnpm -C web lint:fix",
    "typecheck": "pnpm -C web typecheck",
    "format": "prettier . --check",
    "format:write": "prettier . --write",
    "check": "pnpm lint && pnpm typecheck && pnpm -C web format"
  },
  "author": "ozzyonfire",
  "keywords": [
    "shopify",
    "nextjs",
    "polaris",
    "prisma",
    "template"
  ],
  "dependencies": {
    "@iarna/toml": "^2.2.5",
    "dotenv": "^16.6.1",
    "prettier": "^3.8.0",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@parcel/watcher",
      "@prisma/engines",
      "prisma",
      "sharp",
      "unrs-resolver"
    ]
  }
}
